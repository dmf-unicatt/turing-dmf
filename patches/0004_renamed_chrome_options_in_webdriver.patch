diff --git a/engine/tests.py b/engine/tests.py
index dd47cea..a904cc2 100644
--- a/engine/tests.py
+++ b/engine/tests.py
@@ -102,7 +102,7 @@ class LiveTests(StaticLiveServerTestCase, TuringTests):
         chrome_options.add_argument('--window-size=1420,1080')
         chrome_options.add_argument('--headless')
         chrome_options.add_argument('--disable-gpu')
-        self.selenium = webdriver.Chrome(chrome_options=chrome_options)
+        self.selenium = webdriver.Chrome(options=chrome_options)
         self.updated = False
 
         self.user = User.objects.create_user('test', 't@e.st', 'test')
@@ -116,7 +116,7 @@ class LiveTests(StaticLiveServerTestCase, TuringTests):
         chrome_options = webdriver.ChromeOptions()
         chrome_options.add_argument('--no-sandbox')
         chrome_options.add_argument('--window-size=1420,1080')
-        chrome = webdriver.Chrome(chrome_options=chrome_options)
+        chrome = webdriver.Chrome(options=chrome_options)
         url = '{}/engine/'.format(self.live_server_url)
         chrome.get(url)
         input('Premi Invio per continuare...')
@@ -759,7 +759,7 @@ class HtmlTests(StaticLiveServerTestCase, TuringTests):
         chrome_options.add_argument('--window-size=1420,1080')
         chrome_options.add_argument('--headless')
         chrome_options.add_argument('--disable-gpu')
-        self.selenium = webdriver.Chrome(chrome_options=chrome_options)
+        self.selenium = webdriver.Chrome(options=chrome_options)
 
         # Crea un utente di test
         self.user = User.objects.create_user('test', 't@e.st', 'test')
